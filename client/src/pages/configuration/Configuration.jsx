import React from "react";
import { Tabs } from "../../components";

const Configuration = () => {
  return (
    <section className="flex flex-col gap-4">
      <h2 className="sub-heading mb-4">Configuration</h2>
      <Tabs />
    </section>
  );
};

export default Configuration;
